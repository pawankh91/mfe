name: deploy-container

on:
  push:
    branches:
      - master
    paths:
      - 'packages/container/**'

defaults:
  run:
    working-directory: packages/container

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - run: npm install
      - run: npm run build

      - uses: shinyinc/action-aws-cli@v1.2
      - run: aws s3 sync dist s3://mfe-kavi-dashboard/container/latest
        env:
          AWS_ACCESS_KEY_ID: AKIA3JS66B3IL7GSLO24
          AWS_SECRET_ACCESS_KEY: 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
          AWS_DEFAULT_REGION: eu-north-1